//
//  Color+Extensions.swift
//  Promptly
//
//  Created by Matthew Kaulfers on 1/27/22.
//

import SwiftUI

extension Color {
    static let promptlyNavy = Color("promptlyNavy")
    static let promptlyLightNavy = Color("promptlyLightNavy")
    static let promptlyLightGray = Color("promptlyLightGray")
    static let promptlyTeal = Color("promptlyTeal")
    static let promptlyLightTeal = Color("promptlyLightTeal")
    static let promptlyOrange = Color("promptlyOrange")
}
